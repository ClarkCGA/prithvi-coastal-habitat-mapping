name: my_satellite_env  # Change this to your preferred environment name
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pyyaml=6.0.2
  - pytorch>=2.0.0
  - torchvision
  - xarray>=2025.1
  - dask>=2025.1
  - netCDF4
  - bottleneck
  - zarr
  - timm>=1.0.10
  - einops
  - yacs
  - affine
  - tqdm
  - wandb>=0.19
  - pillow
  - rioxarray=0.18.2
  - geopandas
  - pandas>=2.0
  - matplotlib
  - scipy
